'use strict';
// Facility data is maintained in Supabase. This screen only calculates straight-line
// distance locally; it does not load Google Maps Platform libraries or call route APIs.
const KASI_NEARBY_PREFECTURES = ['北海道','青森県','岩手県','宮城県','秋田県','山形県','福島県','茨城県','栃木県','群馬県','埼玉県','千葉県','東京都','神奈川県','新潟県','富山県','石川県','福井県','山梨県','長野県','岐阜県','静岡県','愛知県','三重県','滋賀県','京都府','大阪府','兵庫県','奈良県','和歌山県','鳥取県','島根県','岡山県','広島県','山口県','徳島県','香川県','愛媛県','高知県','福岡県','佐賀県','長崎県','熊本県','大分県','宮崎県','鹿児島県','沖縄県'];
const KASI_NEARBY_PURPOSES = {
  manufacture:{label:'装具の製作',category:'義肢装具製作所・装具店'}, repair:{label:'修理',category:'義肢装具製作所・装具店'}, fitting:{label:'適合確認',category:'装具外来のある医療機関'}, rehabilitation:{label:'リハビリ',category:'理学療法・リハビリ施設'}, consultation:{label:'制度相談',category:'自治体の相談窓口・障害者支援施設'}
};
const KASI_NEARBY_ADDRESS_API = 'https://geolonia.github.io/japanese-addresses/api/ja.json';
const KASI_NEARBY_GEOCODER = 'https://msearch.gsi.go.jp/address-search/AddressSearch';
const nearby$ = id => document.getElementById(id);
let nearbyReady = false, nearbySavedAddress = '';
const nearbyMunicipalities = new Map();

function nearbyOption(value, text) { const option = document.createElement('option'); option.value = value; option.textContent = text; return option; }
function nearbySetStatus(text, error = false) { const status = nearby$('nearby-status'); status.textContent = text; status.classList.toggle('error', error); }
function nearbyDistance(meters) { return meters >= 1000 ? `約 ${(meters / 1000).toFixed(meters >= 10000 ? 0 : 1)} km` : `約 ${Math.round(meters)} m`; }
function nearbyCoordinates(latitude, longitude) { const lat = Number(latitude), lng = Number(longitude); return Number.isFinite(lat) && Number.isFinite(lng) && Math.abs(lat) <= 90 && Math.abs(lng) <= 180 ? {lat, lng} : null; }
function nearbyStraightLineMeters(origin, destination) { const radians = value => value * Math.PI / 180, lat = radians(destination.lat - origin.lat), lng = radians(destination.lng - origin.lng), a = Math.sin(lat / 2) ** 2 + Math.cos(radians(origin.lat)) * Math.cos(radians(destination.lat)) * Math.sin(lng / 2) ** 2; return 6371008.8 * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a)); }
async function nearbyLoadMunicipalities() {
  const prefecture = nearby$('nearby-prefecture').value, municipality = nearby$('nearby-municipality'); municipality.replaceChildren(nearbyOption('', prefecture ? '市区町村を読み込み中…' : '市区町村を選択')); municipality.disabled = true;
  if (!prefecture) return;
  try { let cities = nearbyMunicipalities.get(prefecture); if (!cities) { const response = await fetch(KASI_NEARBY_ADDRESS_API); if (!response.ok) throw new Error('市区町村データを取得できませんでした。'); const payload = await response.json(); cities = (payload[prefecture] || []).map(name => ({name})).filter(city => city.name).sort((a, b) => a.name.localeCompare(b.name, 'ja')); if (!cities.length) throw new Error('市区町村データが見つかりませんでした。'); nearbyMunicipalities.set(prefecture, cities); } municipality.replaceChildren(nearbyOption('', '市区町村を選択'), ...cities.map(city => nearbyOption(city.name, city.name))); municipality.disabled = false; }
  catch (error) { municipality.replaceChildren(nearbyOption('', '市区町村を取得できませんでした')); nearbySetStatus(error.message || '市区町村を取得できませんでした。', true); }
}
function nearbyCurrentPosition() { if (!navigator.geolocation) return Promise.reject(new Error('この端末では現在地を取得できません。登録した住所を選んでください。')); return new Promise((resolve, reject) => navigator.geolocation.getCurrentPosition(position => resolve({lat:position.coords.latitude, lng:position.coords.longitude, label:'現在地'}), () => reject(new Error('現在地を取得できませんでした。位置情報の利用を許可するか、登録した住所を選んでください。')), {enableHighAccuracy:false, timeout:10000, maximumAge:300000})); }
async function nearbyGeocodeAddress(address) { const response = await fetch(`${KASI_NEARBY_GEOCODER}?q=${encodeURIComponent(address)}`); if (!response.ok) throw new Error('登録した住所の位置を取得できませんでした。住所を見直すか、現在地を選んでください。'); const entries = await response.json(), coordinates = nearbyCoordinates(entries?.[0]?.geometry?.coordinates?.[1], entries?.[0]?.geometry?.coordinates?.[0]); if (!coordinates) throw new Error('登録した住所の位置を確認できませんでした。住所を見直すか、現在地を選んでください。'); return {...coordinates, label:address}; }
async function nearbyOrigin(condition) { if (condition.origin === 'current') return nearbyCurrentPosition(); if (!condition.address) throw new Error('登録した住所を入力してから「登録した住所」を選んでください。'); return nearbyGeocodeAddress(condition.address); }
async function nearbyFindFacilities(condition) { const filters = ['select=id,name,address,latitude,longitude,phone,website_url,purpose_codes,prefecture,municipality','is_active=eq.true',`purpose_codes=cs.{${condition.purpose}}`,`prefecture=eq.${encodeURIComponent(condition.prefecture)}`,`municipality=eq.${encodeURIComponent(condition.municipality)}`,'order=name.asc','limit=100']; const rows = await window.KASI_API.select('kasi_nearby_facilities', filters.join('&')); return rows.map(row => ({...row, location:nearbyCoordinates(row.latitude, row.longitude)})).filter(row => row.location); }
function nearbyDirectionsUrl(origin, item) { return `https://www.google.com/maps/dir/?${new URLSearchParams({api:'1', origin:`${origin.lat},${origin.lng}`, destination:`${item.location.lat},${item.location.lng}`}).toString()}`; }
function nearbyRenderResults(items, condition, origin) {
  const container = nearby$('nearby-results'); container.replaceChildren();
  if (!items.length) { const empty = document.createElement('div'); empty.className = 'nearby-empty'; empty.textContent = '条件に合う施設はまだ登録されていません。地域・目的を変えるか、運営者に施設の登録を依頼してください。'; container.append(empty); return; }
  const purpose = KASI_NEARBY_PURPOSES[condition.purpose];
  items.forEach((item, index) => { const article = document.createElement('article'); article.className = 'nearby-card'; const heading = document.createElement('h3'); heading.textContent = item.name; const meta = document.createElement('p'); meta.className = 'nearby-category'; meta.textContent = purpose.category; const details = document.createElement('p'); details.className = 'nearby-details'; details.textContent = `直線距離：${nearbyDistance(item.distanceMeters)}`; const address = document.createElement('p'); address.textContent = item.address; const source = document.createElement('p'); source.className = 'nearby-source'; source.textContent = item.phone ? `電話：${item.phone}` : '電話番号は施設サイトまたはGoogle Mapsで確認してください。'; const links = document.createElement('div'); links.className = 'nearby-links'; if (item.website_url) { const website = document.createElement('a'); website.className = 'resource-link'; website.href = item.website_url; website.target = '_blank'; website.rel = 'noopener noreferrer'; website.textContent = '施設サイトを開く'; links.append(website); } const mapsLink = document.createElement('a'); mapsLink.className = 'resource-link nearby-map-link'; mapsLink.href = nearbyDirectionsUrl(origin, item); mapsLink.target = '_blank'; mapsLink.rel = 'noopener noreferrer'; mapsLink.textContent = 'Google Mapsで経路を確認'; links.append(mapsLink); const rank = document.createElement('span'); rank.className = 'nearby-rank'; rank.textContent = `${index + 1}`; article.append(rank, meta, heading, details, address, source, links); container.append(article); });
}
function nearbyToggleAddressField() { const field = nearby$('nearby-address-field'), button = nearby$('nearby-address-toggle'), opening = field.hidden; field.hidden = !opening; button.setAttribute('aria-expanded', String(opening)); button.textContent = opening ? '住所入力を閉じる' : '住所を入力（任意）'; if (opening) nearby$('nearby-address').focus(); }
function nearbyShowAddressField() { const field = nearby$('nearby-address-field'); if (!field.hidden) return; field.hidden = false; nearby$('nearby-address-toggle').setAttribute('aria-expanded', 'true'); nearby$('nearby-address-toggle').textContent = '住所入力を閉じる'; }
async function nearbyLoadSavedAddress() { const address = await window.KASI_PROFILE?.loadNearbyAddress?.(); nearbySavedAddress = address || ''; if (nearbySavedAddress) { nearby$('nearby-address').value = nearbySavedAddress; nearbyShowAddressField(); } }
async function nearbySaveAddress(address) { if (address === nearbySavedAddress) return; if (!window.KASI_PROFILE?.saveNearbyAddress) throw new Error('住所を保存する準備ができていません。画面を再読み込みしてください。'); await window.KASI_PROFILE.saveNearbyAddress(address); nearbySavedAddress = address; }
async function nearbySearch() {
  const condition = {prefecture:nearby$('nearby-prefecture').value, municipality:nearby$('nearby-municipality').value, address:nearby$('nearby-address').value.trim(), purpose:nearby$('nearby-purpose').value, origin:document.querySelector('input[name="nearby-origin"]:checked')?.value};
  if (!condition.prefecture || !condition.municipality) { nearbySetStatus('探す地域の都道府県と市区町村を選んでください。', true); return; }
  const button = nearby$('nearby-search'); button.disabled = true; nearby$('nearby-results').replaceChildren(); nearbySetStatus('施設と直線距離を確認しています…');
  try { await nearbySaveAddress(condition.address); const [origin, facilities] = await Promise.all([nearbyOrigin(condition), nearbyFindFacilities(condition)]); const nearby = facilities.map(item => ({...item, distanceMeters:nearbyStraightLineMeters(origin, item.location)})).sort((a, b) => a.distanceMeters - b.distanceMeters); nearbyRenderResults(nearby, condition, origin); nearbySetStatus(`${origin.label}から、${KASI_NEARBY_PURPOSES[condition.purpose].label}の登録施設を近い順に${nearby.length}件表示しています。距離は直線距離です。`); }
  catch (error) { nearbyRenderResults([], condition); nearbySetStatus(error.message || '検索できませんでした。', true); }
  finally { button.disabled = false; }
}
function nearbyAddNavigation() { if (document.querySelector('[data-screen="nearby"]')) return; const button = document.createElement('button'); button.type = 'button'; button.className = 'screen-link nearby-nav-link'; button.dataset.screen = 'nearby'; button.innerHTML = '<svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="M12 21s7-6.2 7-12A7 7 0 1 0 5 9c0 5.8 7 12 7 12z"/><circle cx="12" cy="9" r="2.5"/></svg><span class="nav-long">近くで探す</span><span class="nav-short">近く</span>'; button.addEventListener('click', () => navigateTo('nearby')); document.querySelector('.primary-nav').insertBefore(button, document.querySelector('[data-screen="links"]')); }
async function nearbyInit() { if (nearbyReady) return; nearbyReady = true; nearbyAddNavigation(); const prefecture = nearby$('nearby-prefecture'); prefecture.replaceChildren(nearbyOption('', '都道府県を選択'), ...KASI_NEARBY_PREFECTURES.map(name => nearbyOption(name, name))); prefecture.addEventListener('change', nearbyLoadMunicipalities); nearby$('nearby-address-toggle').addEventListener('click', nearbyToggleAddressField); nearby$('nearby-search').addEventListener('click', nearbySearch); try { await nearbyLoadSavedAddress(); } catch (error) { nearbySetStatus(`保存した住所を読み込めませんでした：${error.message}`, true); } }
window.KASI_NEARBY = {init:nearbyInit}; nearbyAddNavigation();
